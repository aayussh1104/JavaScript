// Use const for creating arrays

const fruits=["apple","mango"];
fruits.push("banana");
console.log(fruits);

// using const we cant create another array with the same name and do changes we can only change in that particular array so thats why developers mostly use const in creating an array
