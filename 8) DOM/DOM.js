// DOM
// Document object model
// Document is an object which contains html elements and all those html elemrnts are document in itself we can check that by console.dir 
console.log(window.document);
console.dir(window.document);